# Python IDE for Termux 

a simple python tool to write 
code, it is useful too if you are a newer

# Install 

pkg update && pkg upgrade 

pkg install git

pkg install python3 ruby luarocks (clang will be installed too while installing python)

now, clone the repo:

git clone "url.git"

# run

cd jupyter-charm-

./run

# python setup

pip install colorama 

